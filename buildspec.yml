version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 14
    commands:
      - n 16
      - npm install -g @angular/cli
  build:
    commands:
      - ng build
artifacts:
  files:
    - "Dockerfile"
